# ============================================
# Digital Twin MVP — Environment Configuration
# ============================================

# GitHub OAuth App (create at https://github.com/settings/developers)
# 1. Go to Settings → Developer settings → OAuth Apps → New OAuth App
# 2. Set Homepage URL: http://localhost:3000
# 3. Set Authorization callback URL: http://localhost:3000/api/auth/callback/github
# 4. Request scopes: repo, read:user, user:email
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# OpenAI API Key (https://platform.openai.com/api-keys)
# Must start with sk-
OPENAI_API_KEY=

# Auth.js Secret — generate with: openssl rand -hex 32
AUTH_SECRET=

# Auth.js URL — required for Auth.js v5 to know the callback base URL
AUTH_URL=http://localhost:3000

# App URL (change for production deployment)
NEXT_PUBLIC_APP_URL=http://localhost:3000
